<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.demo.mapper.StudentMapper">   
	<select id="selectStudentList" resultType="com.example.demo.model.Student">                   <!--이게 오버라이딩이래-->
		SELECT *
		FROM STUDENT
	</select>
	<delete id="deleteStudent" parameterType ="hashmap"> 
	<!--[몇개 영향받았나=> resultType="int"!]이젠 안써도 알아서 인자값받을떄는 파라미터타입  근데 여러개 받으려면 맵으로-->
		<!-- xml은 맵을 받으면 안에있는 변수를 뺴서 나열하니까 걍 stuNo(키 이름)라고만 써도된데-->
		DELETE FROM STUDENT WHERE STU_NO = #{stuNo}
	</delete>
</mapper>